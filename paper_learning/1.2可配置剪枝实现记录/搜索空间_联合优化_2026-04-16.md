# 联合优化搜索空间（2026-04-16）

> 面向软硬件协同优化的**结构级**搜索空间。
> 只列**会被 NAS / 联合搜索工具枚举**的维度。
> 微调超参（queue_length, epochs, lr, skip_aux_heads）和算法超参（P4-P7）**不在此范围**——它们是部署工具而非搜索维度。

---

## 一、剪枝搜索空间（Phase B 已定稿）

### 1.1 搜索维度

| 维度编号 | 配置维度 | 搜索变量 | 取值范围 | 分类 |
|:--------:|---------|---------|---------|:----:|
| **P1a** | Encoder FFN 中间维度 | `encoder.ffn_mid_ratio` | {1.0, 0.8, 0.7, 0.6} | **搜索** |
| **P1b** | Decoder FFN 中间维度 | `decoder.ffn_mid_ratio` | {1.0, 0.7, 0.5, 0.3, 0.2} | **搜索** |
| **P9** | 解码器层数 | `decoder.num_layers` | {5, 6} | **搜索** |
| **P11** | Seg head mask MLP | `seg_head.mask_mlp_ratio` | {1.0, 0.7} | **搜索** |

**搜索规模**：|P1a| × |P1b| × |P9| × |P11| = **4 × 5 × 2 × 2 = 80 个结构配置**

### 1.2 边界依据（实测数据）

| 维度 | 下限 | 下限依据 |
|---|:---:|---|
| P1a | 0.6 | 低于 0.6 时 lanes IoU 跌 >50%（Phase B.1 70/80% 实测）|
| P1b | 0.2 | 0.3 和 0.2 AMOTA 相同（饱和），再低无意义 |
| P9 | 5 | 4 层 AMOTA -0.11 断崖，3 层 -0.26 崩溃 |
| P11 | 0.7 | 只测过 0.7；drivable 不变，lanes/crossing -7~10% |

### 1.3 不进入搜索空间的维度（记录原因）

| 维度 | 状态 | 原因 |
|---|---|---|
| P2 attn_proj | 暂缓 | 级联工程未实现，ROI 3.95M |
| P3 head_mid | 暂缓 | ROI 仅 1.9M |
| P8 attn_head | 退出 | MSDA CUDA kernel 不容非标准 head_dim |
| P10 backbone | 外部 | 依赖李星峰 non-DCN checkpoint |
| P12 seg_transformer | 未实现 | 工程 ~1 天 |
| P13 FPN | 未实现 | 工程 ~0.5 天 |
| P14 embed_dims | 未实现 | 全局级联最复杂 |

---

## 二、量化搜索空间（来自 1.1 可配置量化完成报告）

> 源文档：[`paper_learning/1.1可配置量化实现记录/1.1_可配置量化_完成报告.md`](../1.1可配置量化实现记录/1.1_可配置量化_完成报告.md) § 3

### 2.1 搜索维度

| 维度编号 | 配置维度 | 搜索变量 | 取值范围 | 分类 |
|:--------:|---------|---------|---------|:----:|
| **Q1** | Backbone 部署方式 | `backbone_precision` | {PyTorch_FP32, TRT_FP16, TRT_INT8} | **搜索**（需 non-DCN）|
| **Q2** | BEV encoder 精度 | `bev.precision` | {FP16, INT8} | **搜索** |
| **Q3** | BEV encoder 粒度 | `bev.granularity` | {per-tensor, per-channel} | **搜索** |
| **Q4** | BEV encoder 目标 | `bev.target` | {W+A, W-only} | **搜索** |
| **Q5** | 检测头精度 | `heads.precision` | {FP16, INT8} | **搜索** |
| **Q6** | 检测头粒度 | `heads.granularity` | {per-tensor, per-channel} | **搜索** |

**搜索规模**（TRT 可部署）：
- |Q1| × (|Q2| × |Q3| × |Q4|) × (|Q5| × |Q6|)
- 保守（Q1 锁 FP32）：1 × 8 × 4 = **32 种**
- 激进（Q1 全搜）：3 × 8 × 4 = **96 种**

### 2.2 锁定维度（不进入搜索空间）

| 锁定项 | 锁定值 | 依据 |
|---|---|---|
| 对称性 | `symmetric=True` | TRT GPU 硬约束 |
| 校准方法 | `minmax`（搜索）/ `mse`（部署）| 与其他维度无耦合 |
| 下游头量化 | 锁定 INT8 | 已验证不影响 AMOTA |
| V2X 通信 | 锁定 INT8 | 已验证可行 |
| 逐层配置 | 模块级统一 | 逐层收益微乎其微（1.1 § 5.2）|

### 2.3 PyTorch 端（快筛用，不在部署搜索空间内）

| 场景 | 总组合 | 用途 |
|---|:---:|---|
| 全探索（INT4/INT6 + per-group）| 21,952 | 理论上限 |
| 去掉 INT4/INT6 + per-group | **343** | 快筛推荐 |
| 最小（FP16/INT8 二选一） | 8 | 冒烟 |

**分工**：PyTorch 343 种快筛确认 TRT 是 32 还是 96；TRT 端执行部署搜索。

---

## 三、联合搜索空间的真实基数

### 3.1 朴素乘积

|配置类别| 规模 | 来源 |
|---|:---:|---|
| 剪枝（结构）| 80 | §一 |
| 量化（TRT 保守）| 32 | §二 |
| 量化（TRT 激进）| 96 | §二 |
| **剪枝 × 量化（保守）** | **80 × 32 = 2,560** | — |
| **剪枝 × 量化（激进）** | **80 × 96 = 7,680** | — |

### 3.2 去重约束后的可行基数

| 约束 | 过滤原则 | 影响 |
|---|---|---|
| 1 | P9=6 时 `decoder.num_layers` 默认不动 | 去重 → 无变化 |
| 2 | P1a=1.0 且 P1b=1.0 且 P9=6 且 P11=1.0 时等价于 baseline | -1 个配置 |
| 3 | Q1=TRT_INT8 必须先剪 P10 backbone（但我们不搜 P10）| Q1 缩减为 {FP32, FP16}（2 种）|
| 4 | 量化时机固定"剪后重校准"（见 1.1 § 3.6）| 剪枝-量化顺序已定 |
| 5 | 激进 P1a（0.6）+ 激进 Q（BEV INT8 per-tensor W+A）可能精度崩溃 | ~20% 配置理论可剪除（需实测验证）|

**保守约束后**：~**2,000 个**可行联合配置。

### 3.3 Pareto 上实际需要测的配置

分层搜索策略（参考 1.1 § 3.6 搜索策略）：

| 层级 | 规模 | 成本 |
|:---:|:---:|---|
| **Level 0**：剪枝 Pareto 端点 | 5-8 个（Phase B 已基本完成）| — |
| **Level 1**：量化 Pareto 端点（不剪枝）| 4-6 个（1.1 已完成）| — |
| **Level 2**：剪 × 量联合采样 | **6-10 个** | Phase C 核心 |

---

## 四、Phase C 起步实验计划

### 4.1 实验指标协议（每个实验必报）

| 类别 | 指标 | 测量工具 | 备注 |
|---|---|---|---|
| **精度（跟踪）** | AMOTA, AMOTP, MT, ML, IDS | `eval_pkl_amota.py` | 主任务 |
| **精度（检测）** | mAP, NDS | 同上 | |
| **精度（分割）** | drivable/lanes/crossing/contour IoU | 同上 | 多任务权衡 |
| **参数量** | ego_agent params (M), 相对 baseline (%) | 剪枝脚本 | 仅 ego_agent 范围 |
| **模型体积** | .pth / .onnx / .engine 大小 (MB) | `du -b` | 量化效果直接体现 |
| **推理时间（模块级）** | backbone / bev_encoder / track_decoder / seg_head 各模块 ms | `benchmark_latency.py` (CUDA Event) | 已实现 |
| **推理时间（ego 单 agent forward）** | `model.model_ego_agent(**data)` ms（不含数据加载、不含 other_agent、不含 tracker 更新）| **新增 pure_forward 模式** | **本次用户关心的核心指标** |
| **推理时间（TRT engine 端）** | TRT engine forward ms | TRT profiler | Phase C TRT 部分 |

**说明**：PyTorch 端现有 `benchmark_latency.py` 的 e2e 指标包括 MultiAgent（ego + other_agent + tracker），与用户要求的"纯模型推理"不完全一致。需扩展工具加 `--ego-only` 模式。

---

### 4.2 实验阶段划分

Phase C 先做 **基础设施**（指标工具 + TRT 集成），再做 **剪枝 TRT 验证**，最后做 **剪枝 × 量化联合**。

```
Phase C.0 基础设施（0.5 天）── C.1 TRT 集成（0.5 天）── C.2-C.10 联合优化（2-3 天）
```

### 4.3 Phase C.0 基础设施（先决条件）

| ID | 任务 | 工程量 |
|:---:|---|---|
| **C.0.1** | 扩展 `benchmark_latency.py`：加 `--ego-only` 模式，只测 `model_ego_agent.forward()` 纯推理时间 | 30 min |
| **C.0.2** | 扩展 `benchmark_latency.py`：加 `--trt-engine` 模式，接受 TRT engine path 做对应测量 | 1 h |
| **C.0.3** | 新建 `tools/collect_metrics.py`：一键跑完 AMOTA + latency + 参数统计，输出单 JSON | 30 min |

---

### 4.4 Phase C.1 剪枝模型 TRT 集成验证（无量化）

> **目的**：在做联合量化之前，先验证剪枝后模型能正确走完"ONNX 导出 → TRT FP32/FP16 engine 构建 → engine 推理"全链路。这一步是后续所有 TRT 量化实验的前置依赖。

| 实验 ID | 结构 | 微调状态 | TRT 精度 | 验证重点 |
|:---:|:---:|:---:|:---:|---|
| **C.1.1** | S0 (baseline) | 原始 ckpt | FP32 / FP16 | TRT 基线，确认 engine 构建链路 |
| **C.1.2** | **S1** = FFN 60% + q=2 微调（已有 ckpt）| 已微调 | FP32 / FP16 | **剪枝模型能否成功导出 ONNX + 构 engine** |
| **C.1.3** | **S2** = FFN 60% + dec 5 + q=2 微调（需先跑 pytorch 微调）| 待微调 | FP32 / FP16 | 结构剪枝 + 层数剪枝的叠加 TRT 导出 |

**可能遇到的坑（预估）**：
- ONNX 动态维度：剪枝改变了 FFN 的 channel 数，需要 re-export
- TRT 的 optimization profile：可能需要针对新 shape 重建
- `round_to=8` 的对齐检查：确保 new channels 仍是 8 的倍数

**每个实验报告**：精度 7 项 + 参数 + 模型体积 + PyTorch 推理时间 + TRT engine 推理时间。

---

### 4.5 Phase C.2 剪枝 × 量化联合（正式实验矩阵）

> **前置**：C.0 工具就绪，C.1 确认剪枝模型 TRT 可部署。

基于 Phase B 选 3 个代表性结构：

| 结构 ID | P1a | P1b | P9 | P11 | AMOTA（已测）| 理由 |
|:---:|:---:|:---:|:---:|:---:|:---:|---|
| **S0** | 1.0 | 1.0 | 6 | 1.0 | 0.3298 | Baseline，量化对照 |
| **S1** | 0.4 | 0.4 | 6 | 1.0 | 0.3354 | 当前 Pareto 最优 |
| **S2** | 0.4 | 0.4 | 5 | 1.0 | 待 C.1.3 跑 | 叠加 P9 |
| **S3** | 0.8 | 0.3 | 6 | 1.0 | 待后续 | encoder/decoder 解耦 |

| 实验 ID | 结构 | Q1 Backbone | Q2-4 BEV | Q5-6 Heads | 目的 |
|:---:|:---:|:---:|:---:|:---:|---|
| **C.2.1** | S1 | FP32 | **INT8 per-tensor W+A** | FP16 | 剪 × BEV INT8 首发 ⭐ |
| **C.2.2** | S1 | FP32 | INT8 per-channel W+A | FP16 | 粒度敏感度 |
| **C.2.3** | S1 | FP32 | INT8 W-only | FP16 | 保守（仅权重量化）|
| **C.2.4** | S2 | FP32 | INT8 per-tensor W+A | FP16 | 三维联合 |
| **C.2.5** | S2 | FP32 | INT8 per-tensor W+A | **INT8 per-tensor** | 全 INT8 ⭐⭐ |
| C.2.6 | S0 | FP32 | INT8 per-tensor W+A | INT8 per-tensor | 量化 only 对照（可能已存在于 1.1 成果）|
| C.2.7 | S3 | FP32 | INT8 per-tensor W+A | FP16 | 解耦 + 量化（S3 需先微调）|
| C.2.8 | S2 | **TRT_FP16** | INT8 per-tensor W+A | INT8 per-tensor | backbone FP16（需 non-DCN ckpt，最激进）|

### 4.6 时间估算

| 阶段 | 实验数 | 单实验耗时 | 总耗时（1 GPU）| 2 GPU 并行 |
|---|:---:|---|---:|---:|
| C.0 工具开发 | — | — | 2 h | — |
| C.1 TRT 集成（3 组）| 3 | 30 min 导出 + 15 min TRT + 15 min eval + latency | 3 h | 1.5 h |
| C.2 联合（S2 微调 + 7 联合实验）| 8 | 40 min 微调（若需）/ 40 min 量化 + eval | 6 h | 3 h |
| **Phase C 总计** | **~11** | — | **~11 h** | **~6 h** |

### 4.7 期望产出

| 问题 | 由哪个实验回答 |
|---|---|
| 剪枝模型能否无损导入 TRT？| C.1.2 / C.1.3 |
| 剪枝+INT8 量化是否稳定？| C.2.1 vs C.2.6（或 1.1 已有数据）|
| 量化粒度对剪枝后激活的影响？| C.2.1 vs C.2.2 |
| 层数剪枝能否叠加量化？| C.2.4 vs C.2.1 |
| 三者全量化 AMOTA 能守住吗？| C.2.5 |
| encoder/decoder 解耦值得继续推进吗？| C.2.7 vs C.2.1 |

**核心交付**：**剪枝 × 量化 Pareto 前沿**（每点 10+ 项完整指标），TRT engine latency 实测，对标 UPAQ (2025)、QuantV2X (2025)。
