# 3_6_26_交接文档_阶段二_AP稳定测试产物与H800环境阻塞

日期: 2026-06-26

序号: 3

继承文档:

- `2_6_26_交接文档_阶段二_AP稳定测试优先计划.md`
- `RUNBOOK_stage2_h800_server_access_v1_zh.md`

最新补充:

- `4_6_26_交接文档_阶段二_AP稳定微调规则补充.md` 已明确 Phase C stable smoke 的 AP 微调协议、5GPU 分工、数据集口径、默认 finetune 次数和 AP 不要求 GPU idle 的规则。后续启动 AP stable smoke 时以该文件和 `PLAN_stage2_h800_multi_agent_lut_production_v1_zh.md` 的最新 Phase C 规则为准。

## 1. 本轮结论

本轮完成了 AP source contract 的本地产品化产物:

```text
multi_agent/data/stage2_lut_generation_v1/generated/ap_stability_20260626/
```

关键结果:

| 项 | 结果 |
| --- | --- |
| AP source registry | 32 rows |
| canonical AP rows | 10 rows |
| replay gate | pass, 4/4 anchors pass |
| original60 AP source map | 60 rows |
| Phase C 5GPU finetune jobs | 5 queued, but current H800 preflight blocked |
| measured AP safety | 0 条 measured AP 来自 weight-identity/model-fit/prediction |

当前不能直接启动 Phase C 5GPU AP 微调。原因不是 GPU 不空闲，而是 H800 上缺少本计划依赖的 AP 训练环境和 Pyramid_DAIR_m1 checkpoint 栈。强行启动会导致随机初始化、协议错配或直接失败，产生不可 claim 的 AP。

## 2. 已生成产物

### 2.1 文件清单

| 文件 | 作用 |
| --- | --- |
| `scripts/stage2_build_ap_stability_artifacts.py` | AP source registry / canonical rows / replay gate / original60 source map builder |
| `registry/ap_source_registry_v1.jsonl` | 统一分类 AP source: true_import / true_eval / weight_identity_transfer / model_fit_only |
| `rows/ap_anchor_rows_v1.jsonl` | 10 条 canonical AP rows, 只来自 true source |
| `exports/ap_replay_gate_report.json` | base/p50/trap25/mix_d replay gate |
| `exports/ap_stability_summary.md` | 10 个 AP anchor 的人工审查表 |
| `exports/original60_ap_source_map_v1.jsonl` | original60 每个配置的 AP source 状态 |
| `exports/original60_ap_axis_gap_report_v2.json` | original60 AP gap 统计 |
| `jobs/ap_finetune_smoke_queue_v1.jsonl` | Phase C 5 个 5GPU AP finetune smoke job |
| `jobs/ap_finetune_smoke_job_state_v1.jsonl` | 当前 5 个 job 的 preflight_blocked 状态 |
| `quarantine/ap_unstable_or_unclaimable_v1.jsonl` | 当前 5 个 job 的环境阻塞 quarantine |

快速检查命令:

```bash
cd ${V2X_ROOT}
BASE=multi_agent/data/stage2_lut_generation_v1/generated/ap_stability_20260626
python3 -m json.tool "$BASE/exports/ap_replay_gate_report.json"
python3 -m json.tool "$BASE/exports/original60_ap_axis_gap_report_v2.json"
sed -n '1,80p' "$BASE/exports/ap_stability_summary.md"
sed -n '1,40p' "$BASE/exports/original60_ap_source_map_quick_review.md"
```

### 2.2 AP source registry 分类

| source_kind | count | 说明 |
| --- | ---: | --- |
| `true_import` | 19 | stage_a CSV 和已有 ap_anchor_rows import |
| `true_eval` | 7 | B2/B4/p50b2/mix_a 等真实 eval result |
| `weight_identity_transfer` | 5 | pad64/s1_64/s2_128 等权重恒等继承, 不写 measured row |
| `model_fit_only` | 1 | `ap70_loglinear_model`, 只可 report, 不可 measured |

### 2.3 canonical AP rows

| label | width | AP30 | AP50 | AP70 | source_kind | claim_status |
| --- | --- | ---: | ---: | ---: | --- | --- |
| base | [64,128,256] | 0.833159 | 0.791041 | 0.630864 | true_import | provisional_missing_digest |
| p50 | [32,64,128] | 0.818254 | 0.764428 | 0.564132 | true_import | provisional_missing_digest |
| p75 | [16,32,64] | 0.815909 | 0.756669 | 0.529988 | true_import | provisional_missing_digest |
| trap25 | [48,96,192] | 0.824434 | 0.776939 | 0.590472 | true_import | provisional_missing_digest |
| mix_b | [48,64,256] | 0.818581 | 0.780496 | 0.636160 | true_eval | provisional_missing_digest |
| mix_d | [48,128,128] | 0.825772 | 0.785835 | 0.636938 | true_eval | provisional_missing_digest |
| iso_s0 | [48,128,256] | 0.823978 | 0.784323 | 0.629919 | true_eval | provisional_missing_digest |
| iso_s1 | [64,96,256] | 0.830391 | 0.789614 | 0.633599 | true_eval | provisional_missing_digest |
| iso_s2 | [64,128,192] | 0.821267 | 0.783871 | 0.633890 | true_eval | provisional_missing_digest |
| p50b2_136 | [32,64,136] | 0.827085 | 0.787692 | 0.642530 | true_eval | provisional_missing_digest |

说明:

1. 以上 10 条是 canonical AP source rows, 不是本轮 fresh AP eval。
2. `claim_status=provisional_missing_digest` 是因为多数历史 ckpt/config digest 尚未补齐。
3. `mix_b/mix_d/p50b2_136` AP70 高于 base, 必须保留 finetune protocol, 不能写成“剪枝自然提升 AP”的结论。

## 3. Replay Gate

`exports/ap_replay_gate_report.json` 当前结果:

| label | expected AP70 | observed AP70 | abs diff | gate |
| --- | ---: | ---: | ---: | --- |
| base | 0.630864 | 0.630864 | 0.000000 | pass |
| p50 | 0.564132 | 0.564132 | 0.000000 | pass |
| trap25 | 0.590472 | 0.590472 | 0.000000 | pass |
| mix_d | 0.636938 | 0.636938 | 0.000000 | pass |

当前 gate 类型是:

```text
true_source_import_replay_gate_no_fresh_eval
```

这说明 source/import 链路一致, 但还不是 H800 fresh eval replay。H800 AP 环境修好后, 仍应至少对 base/p50/mix_d 做一次 true eval replay。

## 4. Original60 AP Source Map

当前 original60 AP gap:

| ap_source_status | count |
| --- | ---: |
| `finetune_smoke_blocked_environment` | 5 |
| `weight_identity_transfer_available` | 1 |
| `no_claim_missing_source` | 54 |

首批 5 个 Phase C smoke 配置:

| job | label | width | GPU | status |
| --- | --- | --- | ---: | --- |
| ap_ft_01 | s0_024 | [24,128,256] | 0 | preflight_blocked |
| ap_ft_02 | s0_040 | [40,128,256] | 1 | preflight_blocked |
| ap_ft_03 | s0_056 | [56,128,256] | 2 | preflight_blocked |
| ap_ft_04 | s1_048 | [64,48,256] | 3 | preflight_blocked |
| ap_ft_05 | s2_160 | [64,128,160] | 4 | preflight_blocked |

当前 1 个可 transfer 的 original60 配置:

| label | width | transfer source | AP70 | 规则 |
| --- | --- | --- | ---: | --- |
| s1_064 | [64,64,256] | mix_b | 0.636160 | `mix_b -> s1_64` s0 zero-pad weight identity |

注意: weight-identity transfer 仍不能伪装成 fresh eval。进入最终 LUT 时必须保留 `claimable_weight_identity_transfer`。

## 5. H800 Preflight 结果

已确认 H800 连接:

```text
hostname = <PRIVATE_HOST>
remote workdir = ${V2X_ROOT}
```

GPU 状态:

| GPU | 状态 |
| --- | --- |
| 0-7 | `nvidia-smi` 显示 utilization 0%, pmon 无 compute 进程 |

阻塞项:

| 检查项 | 结果 |
| --- | --- |
| `${V2X_HOME}/miniconda3/envs/UniV2X_2.0/bin/python` | missing |
| `${V2X_HOME}/heal_research/HEAL/opencood/tools/train_ddp.py` | missing |
| `${V2X_ROOT}/tools/structural_prune_pyramid.py` | missing on H800 before sync |
| `${V2X_HOME}/heal_research/checkpoints/stage1/Pyramid_DAIR_m1_base_2023_08_14_11_42_29` | missing |
| H800 `python3 import torch` | missing |
| H800 `python3 import numpy` | missing |
| H800 `python3 import opencood` | missing |

远端存在 `${V2X_DATA_ROOT}/V2Xverse_pyramid`, 但它不是当前 AP source contract 使用的 `${V2X_HOME}/heal_research/HEAL + Pyramid_DAIR_m1_*` 训练栈。直接改用 V2Xverse 会造成 protocol mismatch, 不能无审查替代。

## 6. 下一阶段计划

### P0: 修复 H800 AP 训练环境

目标: 让 Phase C 的 5 个 finetune smoke job 从 `preflight_blocked` 回到 `queued`。

需要满足:

1. H800 上存在可用 Python 环境, 至少可以 import `torch`, `numpy`, `yaml`, `opencood`。
2. H800 上存在 HEAL/OpenCOOD 训练代码, 包含 `opencood/tools/train_ddp.py`。
3. H800 上存在 `tools/structural_prune_pyramid.py` 或等价脚本。
4. H800 上存在 Pyramid_DAIR_m1 base checkpoint:

```text
${V2X_HOME}/heal_research/checkpoints/stage1/Pyramid_DAIR_m1_base_2023_08_14_11_42_29
```

本地最小同步体量估计:

| 路径 | 体量 |
| --- | ---: |
| `${V2X_HOME}/heal_research/HEAL` | 约 510 MB |
| base ckpt dir | 约 223 MB |
| `tools/structural_prune_pyramid.py` | 约 24 KB |

但仅同步代码和 ckpt 不够, 还需要 torch/opencood 可运行环境。优先选择复制/恢复已有 conda env, 不建议在当前 H800 上临时用系统 python 安装。

### P1: 恢复 Phase C 5GPU smoke

环境修复后执行:

```bash
cd ${V2X_ROOT}
python3 scripts/stage2_build_ap_stability_artifacts.py \
  --created-at 2026-06-26T00:00:00Z \
  --finetune-preflight-status queued
```

然后按 `jobs/ap_finetune_smoke_queue_v1.jsonl` 启动:

| GPU | label |
| ---: | --- |
| 0 | s0_024 |
| 1 | s0_040 |
| 2 | s0_056 |
| 3 | s1_048 |
| 4 | s2_160 |

每个配置必须完成:

```text
structural_prune_pyramid
-> flat ckpt check
-> train_ddp --half, epoches=31
-> ONNX export
-> TRT FP16 build
-> DAIR-V2X val_1789 AP eval
```

### P2: Fresh Eval Replay

环境修复后, 在 5 个新配置之外, 至少 fresh eval replay:

```text
base, p50, mix_d
```

门限:

```text
abs(AP70_fresh_eval - AP70_expected) <= 0.003
```

若不通过, 停止扩展 AP, 先定位 eval split、ckpt、engine、fallback path 差异。

## 7. 下一次启动命令

```text
/goal 在 ${V2X_ROOT} 中继续 Stage2 LUT AP 稳定测试。先阅读 multi_agent/methods/design/auto-tuning/progress/4_6_26_交接文档_阶段二_AP稳定微调规则补充.md、multi_agent/methods/design/auto-tuning/progress/3_6_26_交接文档_阶段二_AP稳定测试产物与H800环境阻塞.md、PLAN_stage2_h800_multi_agent_lut_production_v1_zh.md 和 RUNBOOK_stage2_h800_server_access_v1_zh.md。当前已完成 AP source registry、10 条 canonical AP rows、import replay gate 和 original60 AP source map；不要重复从零整理。下一步首要任务是修复/确认 H800 AP 训练环境，使 H800 具备可用 torch/numpy/opencood Python、HEAL train_ddp.py、structural_prune_pyramid.py、Pyramid_DAIR_m1 base ckpt 和 DAIR-V2X-C train/val 数据集。环境修复后，把 ap_finetune_smoke_queue_v1.jsonl 中 s0_024/s0_040/s0_056/s1_048/s2_160 从 preflight_blocked 恢复为 queued，并启动 GPU0-4 五卡并行 Phase C smoke。每个新配置默认 1 次 finetune, epoches=31, 使用 DAIR-V2X train split 训练并在 DAIR-V2X val_1789 评测 AP30/AP50/AP70。AP finetune/eval 不要求 GPU 完全空闲, 但必须记录 nvidia-smi/pmon 和显存状态; OOM 或失败只 quarantine 当前配置。不要把 predicted/model-fit/interpolated AP 写成 measured。stop condition: 5 个新配置全部完成 full chain 或全部有明确 quarantine reason, 并生成 ap_stability_summary.md。
```
