# 11_6_28_交接文档_阶段三_Original60补点队列与H800启动计划

日期: 2026-06-28

继承文档:

- `multi_agent/methods/design/auto-tuning/progress/6_27/10_6_28_交接文档_阶段三_FP16INT8_Original60补点收口计划.md`
- `multi_agent/methods/design/auto-tuning/progress/6_27/9_6_28_交接文档_阶段二_NativeINT8Smoke实测结果与完整ONNXRoute下一步.md`
- `multi_agent/data/stage2_lut_generation_v1/generated/original60_quant_20260627/exports/fp16_int8_original60_completion_review_latest.md`
- `multi_agent/data/stage2_lut_generation_v1/generated/original60_quant_20260627/exports/fp16_int8_original60_h800_launch_plan_latest.md`

## 0. 本轮结论

本轮没有完成 original60 的远端 measured 补点, 也没有刷新 canonical 三指标总表为 measured。完成的是下一阶段补点的本地收口入口:

1. 生成 original60 `60 x {fp16,int8}` completion queue, 共 120 个 config-precision jobs。
2. 新增 review/gap 审阅文件, 明确每个 cell 当前缺什么。
3. 将 native INT8 full ONNX runner 从三点 hard-coded route 扩展为可消费 completion queue 的 original60 批量 route。
4. 将 true-FP16 latency runner 从三点 hard-coded route 扩展为可消费 completion queue 的 original60 批量 route。
5. 写出 H800 launch plan, 包含 sanitized sync/preflight/batch001 命令。

H800 measured 补点未启动的原因:

- 当前 shell 没有可用 SSH key。
- `password-based SSH (disabled; use an SSH key)` / `H800_PASSWORD` 环境变量未设置。
- BatchMode 登录返回 `Permission denied (publickey,password)`。
- 已遵守密码规则: 没有把真实密码写入任何新文件、JSON、Markdown 或脚本。

## 1. 新增代码和测试

新增:

```text
framework/stage2/original60_quant_completion.py
framework/tests/test_stage2_original60_quant_completion.py
scripts/stage2_generate_fp16_int8_original60_completion_queue.py
```

更新:

```text
multi_agent/data/stage2_lut_generation_v1/generated/original60_quant_20260627/raw/int8_native_route/stage2_h800_native_int8_full_onnx_route.py
multi_agent/data/stage2_lut_generation_v1/generated/original60_quant_20260627/raw/fp16_true_smoke/stage2_h800_fp16_true_smoke.py
```

关键能力:

- `build_completion_jobs(...)`: 从 canonical latency/energy/AP 三表生成 120 个 FP16/INT8 completion jobs。
- `native_int8_label_widths_from_jobs(...)`: 从 queue 提取需要 native INT8 full ONNX latency/energy 的 label->width, 当前真实队列为 60 个 label。
- `fp16_label_widths_from_jobs(...)`: 从 queue 提取需要 true-FP16 latency/energy 的 label->width, 当前真实队列为 58 个 label。
- INT8 旧 QDQ/TVM VM measured smoke 不再被当作最终完成; queue 标记为 `needs_native_int8_replace`。
- FP16 runner 和 INT8 runner 均新增 `--completion-queue` / `--labels` 入口。

## 2. 新增本地产物

Queue:

```text
multi_agent/data/stage2_lut_generation_v1/generated/original60_quant_20260627/jobs/fp16_int8_original60_completion_queue_v1.jsonl
```

Review:

```text
multi_agent/data/stage2_lut_generation_v1/generated/original60_quant_20260627/exports/fp16_int8_original60_completion_review_latest.md
multi_agent/data/stage2_lut_generation_v1/generated/original60_quant_20260627/exports/fp16_int8_original60_completion_review_latest.json
```

Gap report:

```text
multi_agent/data/stage2_lut_generation_v1/generated/original60_quant_20260627/exports/fp16_int8_original60_gap_report_latest.md
multi_agent/data/stage2_lut_generation_v1/generated/original60_quant_20260627/exports/fp16_int8_original60_gap_report_latest.json
```

H800 launch plan:

```text
multi_agent/data/stage2_lut_generation_v1/generated/original60_quant_20260627/exports/fp16_int8_original60_h800_launch_plan_latest.md
multi_agent/data/stage2_lut_generation_v1/generated/original60_quant_20260627/exports/fp16_int8_original60_h800_launch_plan_latest.json
```

Queue summary:

| item | count |
|---|---:|
| total jobs | 120 |
| FP16 jobs | 60 |
| INT8 jobs | 60 |
| jobs requiring action | 120 |
| native INT8 labels for latency/energy | 60 |
| true-FP16 labels for latency/energy | 58 |
| AP no_claim jobs | 120 |

Axis status counts:

| axis | measured | needs_native_int8_replace | no_claim |
|---|---:|---:|---:|
| latency | 2 | 2 | 116 |
| energy | 2 | 2 | 116 |
| AP | 0 | 0 | 120 |

解释:

- latency/energy 的 2 个 measured FP16 是 `s0_024/s1_048` true-FP16 smoke。
- latency/energy 的 2 个 `needs_native_int8_replace` 是 `s0_024/s1_048` 旧 QDQ/TVM VM INT8 smoke, 不能作为最终 native INT8 完成。
- AP 仍没有 FP16/INT8 合规 measured 数据。

## 3. Runner 当前状态

### 3.1 Native INT8 full ONNX runner

文件:

```text
multi_agent/data/stage2_lut_generation_v1/generated/original60_quant_20260627/raw/int8_native_route/stage2_h800_native_int8_full_onnx_route.py
```

新增参数:

```text
--completion-queue
--labels
--latency-rows-output
--energy-rows-output
```

目标 row:

```text
rows/native_int8_full_onnx_original60_latency_rows_v1.jsonl
rows/native_int8_full_onnx_original60_energy_rows_v1.jsonl
```

当前可以从 queue 提取 60 个 INT8 label, 包含 `s0_024/s1_048`, 不再局限 `base/s0_024/s1_048`。

### 3.2 True-FP16 latency runner

文件:

```text
multi_agent/data/stage2_lut_generation_v1/generated/original60_quant_20260627/raw/fp16_true_smoke/stage2_h800_fp16_true_smoke.py
```

新增参数:

```text
--completion-queue
--labels
--out-jsonl
--run-id
--gpu
--reset-rows
```

目标 row:

```text
rows/fp16_true_original60_latency_rows_v1.jsonl
```

当前可以从 queue 提取 58 个待补 true-FP16 latency/energy label。`s0_024/s1_048` 未进入该队列, 因为当前 canonical 表已有 true-FP16 latency/energy measured smoke。

### 3.3 尚未产品化的 runner

仍缺:

- `rows/fp16_true_original60_energy_rows_v1.jsonl` 的批量 runner。
- `rows/fp16_true_original60_ap_rows_v1.jsonl` 的合规 AP eval/import route。
- `rows/native_int8_original60_ap_rows_v1.jsonl` 的合规 AP eval/import route。

这些不能从当前 backbone latency/energy 推断。

## 4. 验证

已执行:

```text
PYTHONPATH=${V2X_ROOT} python -m unittest framework.tests.test_stage2_original60_quant_completion framework.tests.test_stage2_native_int8_route framework.tests.test_stage2_lut_productization
```

结果:

```text
Ran 55 tests in 5.898s
OK
```

已执行:

```text
python -m py_compile framework/stage2/original60_quant_completion.py framework/stage2/native_int8_full_onnx.py framework/stage2/native_int8_route.py framework/stage2/lut_productization.py framework/tests/test_stage2_original60_quant_completion.py framework/tests/test_stage2_native_int8_route.py scripts/stage2_generate_fp16_int8_original60_completion_queue.py multi_agent/data/stage2_lut_generation_v1/generated/original60_quant_20260627/raw/int8_native_route/stage2_h800_native_int8_full_onnx_route.py multi_agent/data/stage2_lut_generation_v1/generated/original60_quant_20260627/raw/fp16_true_smoke/stage2_h800_fp16_true_smoke.py
```

结果: exit 0。

Secret check:

```text
rg -n "<literal H800 password or accidental password-based SSH (disabled; use an SSH key) assignment>" <new code and new artifacts>
```

结果: 无命中。

## 5. 下一步执行顺序

1. 只在当前 shell 中安全注入 `password-based SSH (disabled; use an SSH key)`; 不写文件、不写命令日志。
2. 按 `fp16_int8_original60_h800_launch_plan_latest.md` 同步 pack 到 H800 `${V2X_DATA_ROOT}/s2_tvm/native_int8_pack`。
3. 在 H800 运行 py_compile + unittest + `nvidia-smi` preflight。
4. 先跑 native INT8 batch001: `frontier_01/frontier_02/frontier_03`。
5. 如果 batch001 三点成功, 按 queue 分批继续 60 个 INT8 labels。
6. 跑 true-FP16 latency batch001: `frontier_01/frontier_02/frontier_03`。
7. 产品化 FP16 energy runner, 再补 `fp16_true_original60_energy_rows_v1.jsonl`。
8. AP 不得伪造; 需要单独实现合规 true eval/import route。
9. 每批完成后 rsync raw/rows 回本地, 再刷新 canonical 三表和 summary。

## 6. 下一轮 /goal

```text
/goal 在 ${V2X_ROOT} 中继续 Stage2 original60 FP16/INT8 补点收口。不要启动 agent team, 单 agent 执行即可。

先阅读:
- multi_agent/methods/design/auto-tuning/progress/6_27/11_6_28_交接文档_阶段三_Original60补点队列与H800启动计划.md
- multi_agent/data/stage2_lut_generation_v1/generated/original60_quant_20260627/exports/fp16_int8_original60_completion_review_latest.md
- multi_agent/data/stage2_lut_generation_v1/generated/original60_quant_20260627/exports/fp16_int8_original60_gap_report_latest.md
- multi_agent/data/stage2_lut_generation_v1/generated/original60_quant_20260627/exports/fp16_int8_original60_h800_launch_plan_latest.md

当前已完成:
- 生成 jobs/fp16_int8_original60_completion_queue_v1.jsonl, 共 120 个 FP16/INT8 jobs。
- native INT8 full ONNX runner 已支持 --completion-queue, 可从 queue 提取 60 个 INT8 original60 labels。
- true-FP16 latency runner 已支持 --completion-queue, 可从 queue 提取 58 个待补 FP16 labels。
- review/gap/launch plan 已写入 exports。
- 55 个相关 unittest 和 py_compile 已通过。

当前未完成:
- 还没有启动 H800 measured batch, 因为当前 shell 没有 password-based SSH (disabled; use an SSH key) 环境变量, BatchMode 登录失败。
- 还没有生成 rows/native_int8_full_onnx_original60_latency_rows_v1.jsonl 或 rows/native_int8_full_onnx_original60_energy_rows_v1.jsonl。
- 还没有生成 rows/fp16_true_original60_latency_rows_v1.jsonl。
- FP16 energy runner、FP16 AP route、INT8 AP route 仍未产品化。
- canonical 三表还没有刷新为新的 measured rows。

硬约束:
- 不写真实 H800 密码; 只允许使用 session-local password-based SSH (disabled; use an SSH key)。
- latency 统一 ms; raw us 进审阅表前必须转换。
- full_network_claim 必须保持 false。
- INT8 必须优先 native full ONNX route; 旧 QDQ/TVM VM smoke 只能作为历史对照。
- AP70 必须来自合规 true eval/import source, 不能 predicted/model-fit/interpolated/TRT reference 冒充 measured。

下一步:
1. 若 password-based SSH (disabled; use an SSH key) 已在 shell 中安全设置, 按 fp16_int8_original60_h800_launch_plan_latest.md 执行 H800 sync + preflight。
2. 跑 native INT8 batch001: frontier_01/frontier_02/frontier_03, 输出 native_int8_full_onnx_original60_latency/energy rows。
3. 成功后分批跑完 60 个 INT8 labels; 失败时写 op-level blocker 并继续其他 label。
4. 跑 true-FP16 latency batch001, 输出 fp16_true_original60_latency_rows_v1.jsonl。
5. 产品化 FP16 energy batch runner, 再补 fp16_true_original60_energy_rows_v1.jsonl。
6. AP 路线单独实现; 没有合规 source 时保持 no_claim/quarantine。
7. 每批 rsync 回本地后刷新 canonical latency/energy/AP 三表、summary、review/gap 和下一份中文交接文档。
```
